root/introcreate.py

gir ve arat;

								float(self.stat[0])/float(statSummary),
								float(self.stat[1])/float(statSummary),
								float(self.stat[2])/float(statSummary),
								float(self.stat[3])/float(statSummary),

deðiþtir;

								float(self.stat[0])/90,
								float(self.stat[1])/90,
								float(self.stat[2])/90,
								float(self.stat[3])/90,

root/introselect.py

gir ve arat;

										float(valueHTH) / statesSummary,
										float(valueINT) / statesSummary,
										float(valueSTR) / statesSummary,
										float(valueDEX) / statesSummary

deðiþtir ve pack'i kapat;

										float(valueHTH) / 90,
										float(valueINT) / 90,
										float(valueSTR) / 90,
										float(valueDEX) / 90

Not: Her 2 kod bloðundaki 90'lar sizin maksimum statünüze göre ayarlanmalýdýr.