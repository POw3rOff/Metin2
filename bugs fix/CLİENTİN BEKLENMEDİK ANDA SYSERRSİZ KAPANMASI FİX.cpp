Client Source/source/PRTerrainLib/TextureSet.cpp

gir ve arat;

bool CTextureSet::SetTexture(unsigned long ulIndex,

kod bloðunun içinde bul;

	if (!pResource->IsType(CGraphicImage::Type()))
	{
		TraceError("CTerrainImpl::GenerateTexture : %s is NOT Image File", pResource->GetFileName());
		return false;
	}

altýna ekle;

	if (!pResource)
		return false;

tekrar arat;

bool CTextureSet::AddTexture(const char * c_szFileName,

kod bloðunun içinde bul;

	if (!pResource->IsType(CGraphicImage::Type()))
	{
		LogBox("CTerrainImpl::GenerateTexture : ÀÌ¹ÌÁö ÆÄÀÏÀÌ ¾Æ´Õ´Ï´Ù. %s", pResource->GetFileName());
		return false;
	}

altýna ekle ve derle;

	if (!pResource)
		return false;