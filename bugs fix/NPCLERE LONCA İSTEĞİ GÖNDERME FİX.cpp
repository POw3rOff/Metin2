Game-Db Source/Server/game/src/input_main.cpp

gir ve arat;

				if (!ch->IsPC())
					return SubPacketLen;

üstüne ekle ve derle;

				if (!ch->IsPC() || !newmember->IsPC())
					return SubPacketLen;