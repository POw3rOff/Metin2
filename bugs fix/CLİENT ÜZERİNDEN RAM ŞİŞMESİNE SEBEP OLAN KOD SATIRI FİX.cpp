Client Source/source/UserInterface/PythonCharacterManager.cpp

gir ve arat;

			if (pkInstEach->IsForceVisible())
			{
				dwForceVisibleInstCount++;
				continue;
			}

deðiþtir ve derle;

/*			if (pkInstEach->IsForceVisible())
			{
				dwForceVisibleInstCount++;
				continue;
			}*/